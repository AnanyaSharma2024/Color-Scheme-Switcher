# Color Scheme Switcher 🎨

The Color Scheme Switcher is a simple JavaScript project that allows users to change the background color of a webpage by clicking on different color buttons. This project is useful for beginners to understand DOM manipulation and event handling in JavaScript.

## 🚀 Features
- Change background color with a single click
- Uses `addEventListener` for handling click events
- Clean and simple UI
- Beginner-friendly project

## 🛠️ Technologies Used
- HTML5
- CSS3
- JavaScript (DOM Manipulation)

## 📂 Project Structure
├── index.html
├── style.css
└── script.js

pgsql
Copy code

## ⚙️ How It Works
- Each color box is a clickable element.
- When a user clicks on a color, JavaScript detects the click event.
- The background color of the page changes according to the selected color.

## ▶️ How to Run
1. Download or clone the repository
2. Open `index.html` in your browser
3. Click on any color box to change the background color

## 📌 Learning Outcomes
- Understanding `querySelectorAll`
- Working with event listeners
- Using event objects (`e.target`)
- Basic CSS styling
